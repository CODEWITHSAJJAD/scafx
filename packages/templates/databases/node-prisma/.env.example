# Database Connection URL (Prisma)
DATABASE_URL="{{= it.database === 'postgres' ? 'postgresql://postgres:postgres@localhost:5432/' + it.projectName + '?schema=public' : (it.database === 'mysql' ? 'mysql://root:password@localhost:3306/' + it.projectName : 'file:./dev.db') }}"
